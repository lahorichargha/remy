package com.tastycode.remy.commonobjects.dialog;

import com.tastycode.remy.commonobjects.recipe.HasText;

/**
 * The system output, could be just text, or possibly
 * more information, such as 
 * 
 * this could be an abstract concept, not just text
 * but now it is text
 */
public interface WhatToSay extends HasText
{
}
